# Godot 4 local import cache and editor state
.godot/
*.translation

# OS / editor junk
.DS_Store
Thumbs.db
*~
*.swp

# Exports (keep export_presets.cfg if we add one later)
export/
build/
android/build/

# Local user data should never be committed (runtime uses user://)
*.ckey
